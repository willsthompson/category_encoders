[project]
name = "dlt-csv-postgres-pipeline"
version = "0.1.0"
description = "A dlt pipeline that loads CSV/Excel files into PostgreSQL"
requires-python = ">=3.10"
dependencies = [
    "dlt[postgres]>=0.4.0",
    "openpyxl>=3.0.0",
]

[project.scripts]
load-data = "pipeline:main"
